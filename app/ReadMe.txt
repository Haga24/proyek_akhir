1. Buat Virtual env "python -m venv appenv"
2. Masuk ke env "cd appenv"
3. Copy Folder App ke app env
4. aktivkan appenv melalui cmd "scripts\activate"
5. Install Flask "pip install Flask"
6. Run app.py
